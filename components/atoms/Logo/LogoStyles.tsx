import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  mainLogo: {
    display: "flex",
    justifyContent: "flex-start",
    alignItems: "center",
    width: 96,
    height: 27,
  },
});
