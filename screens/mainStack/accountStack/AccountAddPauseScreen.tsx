import React from "react";
import { StyleSheet, Text, View } from "react-native";

type Props = {};

const AccountAddPauseScreen = (props: Props) => {
  return (
    <View>
      <Text>AccountAddPauseScreen</Text>
    </View>
  );
};

export default AccountAddPauseScreen;

const styles = StyleSheet.create({});
